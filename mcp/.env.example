# PermitMonkey MCP server — environment variables.
#
# v0.1: only PORT is read. Future versions may add CIMD client metadata
# (master playbook §104 Pattern 5) when the corrections-flow paid tier
# lands and OAuth-protected tools are added.

PORT=8787
