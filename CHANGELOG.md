# Changelog

All notable changes to **PermitMonkey** are recorded here. Format follows [Keep a Changelog](https://keepachangelog.com/en/1.1.0/) and the project loosely follows [Semantic Versioning](https://semver.org/) (pre-1.0; expect breaking changes between minor entries).

The product is an AI ADU permit assistant for Massachusetts. The MA pivot from a California ADU project (the upstream `mikeOnBreeze/cc-crossbeam` Anthropic Opus 4.6 hackathon winner) is in progress. Entries before 2026-05-03 in this file are backfilled from notable git commits; from 2026-05-04 onward each working session adds an entry.

## How to Update This File

When you finish a working session that ships user-visible, behavioural, or architectural change, add a top-level entry under `## [Unreleased]` (or open a new dated section if a release was cut). Group entries under: `Added`, `Changed`, `Fixed`, `Removed`, `Security`, `Docs`, `Infra`. Reference commit short SHAs (`abc1234`) so the entry stays linkable. One sentence per bullet, written so a non-engineer reading from cold can understand what shipped.

Don't list every commit. List things a reasonable person would want to know about: new flows, new skills, new reference files, regulatory citation changes, eval-harness milestones, breaking architectural decisions, security fixes, documentation that future Claude sessions need to find.

---

## [Unreleased]

### Added

- **`massachusetts-adu` skill bumped 8 → 12 reference files** (closes deferred Phase 1 deliverable). Four new state-code references with inline provenance tags throughout: [`stretch-energy-code-225-cmr-22.md`](server/skills/massachusetts-adu/references/stretch-energy-code-225-cmr-22.md) (three-tier energy code structure, three Specialized Opt-In compliance pathways, Climate Zone 5 envelope targets, common corrections); [`fire-prevention-527-cmr.md`](server/skills/massachusetts-adu/references/fire-prevention-527-cmr.md) (527 CMR 12 electrical / NEC 2023 / AFCI / GFCI; MGL Ch 148 §§26F/26F½ smoke and CO alarm requirements with new-construction power-source rules; 527 CMR 1 sprinklers and apparatus access); [`accessibility-521-cmr.md`](server/skills/massachusetts-adu/references/accessibility-521-cmr.md) (MAAB single-family scoping default, 30%-of-FCV work-cost threshold, variance procedure with 4-12 week timing); [`plumbing-248-cmr.md`](server/skills/massachusetts-adu/references/plumbing-248-cmr.md) (248 CMR 10 plumbing + 248 CMR 4-7 gas fitting; four-permit ADU workflow building/plumbing/gas/electrical; HPWH coordination at the energy/plumbing/electrical intersection). SKILL.md reference catalog updated. Strict eval gate remains green (all four fixtures still precision=1.0 / recall=1.0 / verdict=✓). (commit pending)
- **AgentRunner subprocess bridge** — `--mode=agent` is no longer a stub. [`agents-permitmonkey/src/cli.ts`](agents-permitmonkey/src/cli.ts) is a small dispatcher that reads a fixture file, picks the right flow (`runMAEligibility` for `eligibility-check`; `corrections-letter` and `plan-review` deferred to test-asset gating), runs it under a timestamped session directory, and prints a single-line JSON result on stdout. [`AgentRunner`](server/evals/runner.ts) spawns this CLI as a Node subprocess (`--experimental-strip-types`, separate cwd), captures stdout/stderr, surfaces failures cleanly into `agent_errors`, and reads the agent's canonical output file as `RunOutput.raw_markdown`. Cross-package coupling is runtime-only — server keeps zero compile-time dependency on `agents-permitmonkey/`, and CI mock-mode is unchanged. Verified by exercising the agent-mode path without `npm install`: the CLI fails gracefully at module resolution and surfaces `ERR_MODULE_NOT_FOUND` into the report's `agent_errors` array. Once `npm install` in `agents-permitmonkey/` + `ANTHROPIC_API_KEY` are in place, `npm run eval -- --mode=agent --filter=eligibility-boston-back-bay` runs the live agent and scores it against the same Method-1-verified ground truth that mock mode uses. (commit pending)
- **Third real eval fixture + strict CI gate** — [`server/evals/fixtures/plan-review-boston-site-plan-deficiencies/fixture.json`](server/evals/fixtures/plan-review-boston-site-plan-deficiencies/fixture.json) closes Flow 3 (City Pre-Screening) coverage: a Back Bay site plan is missing setback dimensions, the parking compliance table, and an MBTA proximity demonstration. Mock-mode output quotes verbatim sentences from `adu-plan-review/references/checklist-site-plan.md` and `massachusetts-adu/references/conflicts-and-preemption.md`. Scores **precision=1.0 (3/3), recall=1.0 (2/2), verdict_match=true**. Same skill-quoting pattern as the prior two fixtures. **All three production flows now have audit-graded fixtures.** [`defaultCannedMarkdown`](server/evals/run.ts) was updated to also use a verbatim skill quote so `smoke-mock` lifts from precision=0.0 to 1.0, giving the strict gate full headroom. The evals workflow ([`.github/workflows/evals.yml`](.github/workflows/evals.yml)) now runs with `--strict` — CI fails if `citation_precision_p50 < 0.95` or `verdict_accuracy < 0.9`. (commit pending)
- **Second real eval fixture: `corrections-boston-parking-preempted`** at [`server/evals/fixtures/corrections-boston-parking-preempted/fixture.json`](server/evals/fixtures/corrections-boston-parking-preempted/fixture.json) — Flow 1 (Corrections Letter Interpreter) coverage. City plan checker requires 1 off-street parking space for an ADU within 0.4 mi of MBTA Hynes Convention Center; the agent must recognize the item as state-preempted under MGL Ch 40A § 3 paragraph 2, accept the unrelated corrections (egress / smoke alarm / energy-code), and draft a professional response. Mock-mode output quotes four verbatim sentences from `chapter-150-of-2024.md`, `conflicts-and-preemption.md`, and `transit-parking.md`. Scores **precision=1.0 (4/4), recall=1.0 (2/2 required authorities), verdict_match=true**. Same skill-quoting pattern documented in the Boston Back Bay fixture commit. Both Flow 1 and Flow 2 now have audit-graded fixtures. (commit pending)
- **First real eval fixture: `eligibility-boston-back-bay`** at [`server/evals/fixtures/eligibility-boston-back-bay/fixture.json`](server/evals/fixtures/eligibility-boston-back-bay/fixture.json). 5,000 sqft Back Bay parcel, 1,800 sqft primary, 800 sqft attached ADU, within 0.5 mi of MBTA, in a Landmarks district — exercises the canonical Boston scenario where state law preempts USE but the Landmarks Commission still governs FORM. Mock-mode output quotes verbatim sentences from `massachusetts-adu/references/chapter-150-of-2024.md` and `boston-adu/references/transit-parking.md` so all three citations verify via Method 1 (skill reference lookup) without network access. Scores **precision=1.0, recall=1.0, verdict_match=true** in mock mode; passes `--strict` gate. To support per-fixture canned outputs, extended the [`Fixture`](server/evals/types.ts) interface with optional `mock_output: string` (backwards-compatible — `smoke-mock` still falls back to the runner's default canned markdown via the constructor path). [`MockRunner`](server/evals/runner.ts) prefers `fixture.mock_output ?? this.cannedMarkdown`. [`AgentRunner`](server/evals/runner.ts) stub message updated to enumerate the kind→flow dispatch table (eligibility-check → `runMAEligibility`, corrections-letter → `runCorrectionsAnalysis`, plan-review → `runPlanReview`); actual cross-package import remains pending the workspace decision. (commit pending)
- **Frontend test suite wired to CI** — extended [`frontend/lib/eligibility.test.ts`](frontend/lib/eligibility.test.ts) from 5 to 15 `node:test` cases covering all verdict branches (`likely_eligible` / `needs_review` / `not_eligible`), all five covered city profiles (Boston, Cambridge, Somerville, Newton, Brookline), historic-overlay downgrade logic, the parking exemption null path, citation array shape (MGL Ch 40A §3 and 760 CMR 71.00 always present), case-insensitive address parsing, and the uncovered-city fallback. Added `npm test` script and a new workflow at [`.github/workflows/frontend-tests.yml`](.github/workflows/frontend-tests.yml) that runs the suite on any `frontend/**` change. 15/15 passing locally. (commit pending)
- **MA Eligibility Agent** at [`agents-permitmonkey/src/flows/ma-eligibility.ts`](agents-permitmonkey/src/flows/ma-eligibility.ts) — Generator-Verifier pair runnable via the Agent SDK. Generator (Sonnet) invokes `adu-eligibility-checker` + `massachusetts-adu` + `ma-city-research` (or `boston-adu` when city is Boston) to produce an eligibility verdict JSON with an inline `citations` array. Verifier (Opus) audits every citation against either a skill reference file (Method 1) or canonical URL (Method 2) and writes both an audit JSON and a human-readable summary Markdown. Output contract extends the existing eligibility-checker shape with the audit fields, so the eventual `/eligibility` frontend route can render the same `verified-skill` / `verified-url` / `unverified` / `broken` pills the corrections viewer already uses. Smoke test at [`agents-permitmonkey/src/tests/test-l3e-ma-eligibility.ts`](agents-permitmonkey/src/tests/test-l3e-ma-eligibility.ts) exercises the full pair against a hardcoded Boston Back Bay fixture (Sonnet+Opus, ~$0.50–2.00 per run). Closes the Phase 2 roadmap deliverable to wire the three skills into one runnable agent. (commit pending)
- **5 sheet-type plan-review checklists** at `server/skills/adu-plan-review/references/` — Phase 2 Week 4-5 deliverable. Each checklist gives the city-pre-screening agent line-item required elements with code citations, common deficiencies, confidence calibration, and a quick-review decision tree. Pattern follows the existing `checklist-cover.md` (1,356 new lines total): `checklist-site-plan.md` (277 lines: lot boundary, setbacks, lot coverage / FAR, parking with MBTA proximity workflow, drainage, utilities, historic district indication, wetlands), `checklist-floor-plan.md` (248 lines: existing/proposed plans, ADU dwelling-unit completeness, size compliance, egress, fire separation, alarms, ventilation, accessibility), `checklist-elevations.md` (251 lines: building height, exterior wall fire rating, roof plan, solar-ready provisions, sections, envelope R-values, window/door schedule, historic district design), `checklist-structural.md` (242 lines: PE stamp, foundation, framing, snow/wind, lateral resistance, connections, structural notes, existing-conditions assessment — high `[REVIEWER:]` blank rate by design), `checklist-mep-energy.md` (338 lines: plumbing DWV, hot water with Specialized Opt-In pathway, mechanical with HRV/ERV requirements, electrical with NEC 2023 / 527 CMR 12, AFCI/GFCI, energy code compliance pathway, envelope documentation, solar PV). Lint baseline rose from 241 → 272 across 36 files (+31 from new content; expected given checklist code-density). (commit pending)
- **Provenance lint rule** at [`server/evals/provenance-lint.ts`](server/evals/provenance-lint.ts) — Week 4 cross-cutting reliability milestone. Scans skill `references/` and `decision-tree/` markdown trees for sentences that look like cited factual claims (MGL chapter references, CMR references, "Chapter N of the Acts" citations, statutory section symbols, Boston Article numbers, IRC/IBC/NEC sections) and warns when no inline `[source: URL | retrieved: DATE | citation: SECTION]` tag is found within ±250 characters. Exemptions: YAML frontmatter, fenced code blocks, paragraphs containing "TBD" or "verification pending", and `## See Also` / `## Source Maintenance` / `## Reference File Catalog` / `## Open Items` sections. Runs as `npm run lint:provenance` in `server/`. Wired into [`.github/workflows/evals.yml`](.github/workflows/evals.yml) as a warn-only job (no `--strict` flag) so it doesn't fail CI while existing skill content is incrementally tagged. **Baseline: 241 violations across 31 files** as of this commit; flip to `--strict` once the count hits zero on main. (commit pending)
- **`context-housing-150-2024.md`** and **`energy-stretch-code.md`** Boston reference files — bringing Boston deep skill to **11 of 12** reference files. `context-housing-150-2024.md` documents the policy backdrop: why MA passed Ch. 150, how Boston is implementing it via the Neighborhood Housing Zoning initiative (Mattapan Feb 2024 + Roslindale/West Roxbury/Hyde Park drafts in progress), and what voice the agent should adopt in contractor responses about the policy context. `energy-stretch-code.md` covers the Specialized Opt-In Energy Code (225 CMR 22 Appendix RC) as applied to Boston ADUs: three compliance pathways (all-electric, mixed-fuel + solar PV offset, Passive House), envelope R-value targets, HERS rating expectations (45-55), HRV/ERV requirements, solar-ready provisions, and the seven most common Boston ADU energy corrections. Only `ordinance-adu-standards.md` remains — blocked on Municode browser access for Mattapan amendment text. (commit pending)
- **Production wiring for `verify-citations` server action** via [`frontend/next.config.ts`](frontend/next.config.ts) — adds `outputFileTracingRoot` (resolves to repo root) and `outputFileTracingIncludes` rule that bundles `server/skills/**/*.md` into the deployed Vercel function. Without this, Method 1 (skill reference lookup) silently fails in production and every verified citation falls through to Method 2 only. Build verified clean. (commit pending)

### Changed

- **`frontend/app/page.tsx` replaced** — the landing is now MA-pivoted, ported from the design synthesis at [`docs/design/landing-stripe-synthesis.html`](docs/design/landing-stripe-synthesis.html). Removes 698 lines of CA-era copy ("28 reference files of CA law", "480+ cities supported", "Hackathon 2026" badge, "Built in 7 days · Huntington Beach, CA" footer). New structure: gradient-mesh hero with two floating Provenance UI cards, demo video preserved, city coverage strip (Boston / Cambridge / Somerville / Newton / Brookline + statewide floor), 4-card stat strip (9/12 refs, 100% verified, <15min, 3 flows), three-flow grid (Corrections / Checklist / Pre-Screening), live Provenance UI showcase using the actual `<CitationPill>` and `<CitationPanel>` components, MA differentiation section with code block of the inline provenance tag format, three-tier pricing ($0 / $99 Pro / $499 Firm), free-tool eligibility-checker CTA, MA-honest footer. (commit pending)
- **`frontend/components/landing-provenance-demo.tsx`** added — client-component wrapper that renders a sample corrections-response markdown through `MarkdownWithCitations` with three pre-assigned citation states (verified-skill / verified-url / verified-skill) so the landing demonstrates the actual moat above the fold. Eats the dog food. (commit pending)
- **`frontend/app/globals.css`** extended with landing-page utility classes: `.hero-mesh`, `.topo-dots`, `.card-float`, `.gradient-text`, `.stat-num`, `.eyebrow`, `.marquee-mask`, `.code-block-moss`, `.landing-pill` (+ `-coral`, `-amber` variants). Token-based — uses the existing CSS variable palette, no hardcoded colors. (commit pending)

### Added

- **Landing page design synthesis** at [`docs/design/landing-stripe-synthesis.html`](docs/design/landing-stripe-synthesis.html) — standalone single-file Tailwind-via-CDN landing page that combines Stripe's structural polish (mesh-gradient hero, stat strip, multi-section feature breakdown, code showcase, sticky CTAs), Magic Dirt v2 design tokens (moss green primary, warm earth gradient, photoreal ADU miniatures, Playfair Display + Nunito), and cc-crossbeam structural DNA (three-flow card layout, "built with Claude" attribution). Reflects the actual MA pivot — replaces the existing landing's CA-era copy ("28 reference files of CA law", "480+ cities supported") with MA-specific positioning. [`docs/design/landing-stripe-synthesis-rationale.md`](docs/design/landing-stripe-synthesis-rationale.md) documents what was kept, what was changed, and the port-to-Next.js plan. (commit pending)
- **`fees-impacts.md`** and **`fire-boston.md`** Boston reference files — bringing Boston deep skill to **9 of 12** reference files. `fees-impacts.md` covers ISD building permit fees (approximate), Article 80 linkage thresholds (single ADU exempt), Boston Inclusionary Development Policy applicability (single ADU exempt), and the impact-fee preemption analysis under the Boston special-act framework. `fire-boston.md` covers BFD plan-review scope, sprinkler triggers (state floor doesn't require IRC R313 in MA, but extension required if primary is sprinklered), apparatus access for rear-of-lot ADUs, fire flow in outer-neighborhood projects, fire separation IRC R302.3 for attached ADUs, and the construction-stage vs. sale-stage layers of MGL Ch 148 §§26F/26F½ smoke and CO alarm requirements. (commit pending)
- **Provenance UI Phase B verification** — server action [`frontend/app/actions/verify-citations.ts`](frontend/app/actions/verify-citations.ts) extracts inline citation tags and runs Method 1 (skill reference lookup) + Method 2 (canonical URL fetch) before the client renders. [`frontend/lib/citations/verify.ts`](frontend/lib/citations/verify.ts) is the frontend port of the server's `verifyCitation()`, with shared error codes so eval-harness scoring and UI verification agree. `results-viewer.tsx` calls the server action when output loads, populating real `verified-skill` / `verified-url` / `unverified` / `broken` statuses on every pill. Falls back to `pending` while in flight, `unverified` if the action throws. (commit pending)
- **`historic-districts.md`** and **`eohlc-compliance.md`** Boston reference files. `historic-districts.md` enumerates all 11 Boston Landmarks Commission districts (Beacon Hill, Back Bay, Bay State Road/Back Bay West, St. Botolph, South End, Bay Village, Mission Hill Triangle, Aberdeen, Fort Point Channel, Highland Park, plus the Charlestown Monument Square study) with designation dates, district types (Landmark District vs. Architectural Conservation District), and the practical implication that Landmarks review is NOT preempted by Ch. 150. `eohlc-compliance.md` documents the open legal question of how Ch. 150 reaches Boston's special-act zoning enabling framework, the two readings in the practitioner community, and the agent's hybrid-reading default for contractor responses. (`e4e7002`)
- **`results-viewer.tsx` wired** to the Provenance UI — replaces `<ReactMarkdown>` with `<MarkdownWithCitations>`, wraps in `<CitationPanelProvider>`, mounts `<CitationPanel>`. Phase A: extract-only, all pills render `unverified` so the contractor knows to click through to the source manually. (`36c9cfd`)
- **Provenance UI Phase A** — clickable inline citation pills and a side-panel viewer for cited claims in agent output ([`frontend/components/citation-pill.tsx`](frontend/components/citation-pill.tsx), [`citation-panel.tsx`](frontend/components/citation-panel.tsx), [`markdown-with-citations.tsx`](frontend/components/markdown-with-citations.tsx)). Five status states: verified via skill reference, verified via canonical URL, unverified, broken source, pending. Demo at `/design-sandbox/citations`. Wires to the existing `verifyCitation()` service from the route handler in a follow-up commit. (`0026920`)
- **Provenance UI design spec** at [`docs/design/provenance-ui.md`](docs/design/provenance-ui.md). (`29f88c7`)
- **Claude design prompt** at [`docs/claude-design-prompt.md`](docs/claude-design-prompt.md) — repo-wide context primer that loads the full project picture (product, ICP, voice, differentiation, design tokens, what to design, what not to design) into a fresh Claude session. (`d6941fb`)
- **Eval harness skeleton** at [`server/evals/`](server/evals/) — discovers fixtures under `server/evals/fixtures/`, runs each through a `Runner` (Mock or Agent), scores citation precision via the existing `verifyCitation()` service, citation recall against fixture-declared expectations, verdict match, and latency. JSON report per run. CI workflow at [`.github/workflows/evals.yml`](.github/workflows/evals.yml). 7/7 unit tests passing for the citation extractor. (`a226a10`)
- **`server/skills/boston-adu/`** — Boston deep-skill scaffold with 5 of 12 reference files: [`zoning-residential.md`](server/skills/boston-adu/references/zoning-residential.md), [`ordinance-adu-rules.md`](server/skills/boston-adu/references/ordinance-adu-rules.md), [`transit-parking.md`](server/skills/boston-adu/references/transit-parking.md) (the MA differentiator — MBTA proximity), [`permit-process.md`](server/skills/boston-adu/references/permit-process.md), [`building-codes.md`](server/skills/boston-adu/references/building-codes.md). All reference files use the new inline provenance tag format `[source: URL | retrieved: DATE | citation: SECTION]`. (`f1897cc`, `0563119`, `01f2bb6`)
- **"Editing Discipline (Karpathy)"** section folded into [`.claude/CLAUDE.md`](.claude/CLAUDE.md) at slot #3 (after Non-Negotiables, before Opus 4.7 Calibration) and mirrored to [`AGENTS.md`](AGENTS.md). Source: `forrestchang/andrej-karpathy-skills`. (`7099d05`)

### Changed

- **Boston ADU implementation framing** corrected throughout `server/skills/boston-adu/`. Boston operates under a special-act zoning enabling framework, not standard MGL Ch 40A. Boston implements Ch. 150 of the Acts of 2024 neighborhood-by-neighborhood through BPDA's "Neighborhood Housing Zoning" initiative — Mattapan adopted Feb 2024; Roslindale, West Roxbury, Hyde Park drafts in progress as of 2026-05-03. Removed five unverified "Article 26A" citations originally projected from generic ADU naming conventions. (`0563119`)
- **780 CMR edition** corrected from 9th to **10th Edition** (effective Oct 2023, IRC 2021 / IBC 2021 base) in four places in [`server/skills/boston-adu/SKILL.md`](server/skills/boston-adu/SKILL.md). Stale "9th ed." originated in [`.claude/skills/ma-city-research/references/boston.md`](.claude/skills/ma-city-research/references/boston.md) (last_verified 2026-04-22) and propagated. The state skill `780-cmr-essentials.md` is the authoritative source. Established a project-wide rule: when state skill and city-research disagree on a state-code fact, trust the state skill. (`01f2bb6`)

### Removed

- Side branch `chore/add-karpathy-claude-md` retired both locally and on `origin`. Content folded into `.claude/CLAUDE.md`. (`7099d05`)

### Docs

- **Lab Notes (`.claude/CLAUDE.md`)** extended with two citation-discipline failures from this session and the corresponding fixes (Article 26A projection, 780 CMR edition propagation). Each Lab Note documents the failure, root cause, fix, and a generalised rule. (`0563119`, `01f2bb6`)

### Infra

- `frontend/package-lock.json` renamed `crossbeam-frontend` → `permitmonkey-frontend` to match `package.json` (leftover from the earlier CA→MA rename). (`0026920`)
- `.gitignore` adds `server/evals/reports/` (eval report artifacts are uploaded as CI artifacts, not committed). (`a226a10`)

---

## [Pre-changelog baseline] — 2026-05-03

This entry collapses commits before this changelog was introduced. The full git log remains the authoritative record; entries below highlight what's load-bearing for future sessions.

### Added (pre-baseline)

- **Week 0 preflight** ([`a807b90`](.)) — repo migrated off OneDrive to `C:\dev\permitmonkey` to escape `node_modules` corruption from sync races. Saved master AI playbook at [`docs/master-ai-playbook.md`](docs/master-ai-playbook.md) (5,681 lines). Added on-demand index skill at [`.claude/skills/ai-playbook-lookup/SKILL.md`](.claude/skills/ai-playbook-lookup/SKILL.md). Scoped [`frontend/CLAUDE.md`](frontend/CLAUDE.md) (Next.js 16 + React 19 + Supabase SSR rules) and [`server/CLAUDE.md`](server/CLAUDE.md) (Cloud Run orchestrator + Vercel Sandbox + cache discipline). Header on [`PLAYBOOK.md`](PLAYBOOK.md) clarifying scope vs. master playbook.
- **MA eligibility checker** + **injection pre-scan** + **citation verification service** ([`7457154`](.)) — `server/src/services/citation-verification.ts` with Method 1 (skill reference cross-check) and Method 2 (canonical URL fetch).
- **MA pivot expansion** ([`c7b9b5f`](.)) — new MA-specific skills, regulatory references for Boston/Cambridge/Somerville/Newton/Brookline, SQL schema, security hardening.
- **Pivot from California to Massachusetts; rebrand to PermitMonkey** ([`28efe94`](.)) — first move off the upstream cc-crossbeam CA codebase. California content retained under `_legacy/` for regression reference.

### Notes

- Project pivots from California to Massachusetts driven by Chapter 150 of the Acts of 2024 (Affordable Homes Act, §§7-8 amending MGL Ch 40A §§1A and 3) — statewide by-right ADUs effective February 2, 2025.
- The upstream repository is [github.com/mikeOnBreeze/cc-crossbeam](https://github.com/mikeOnBreeze/cc-crossbeam) (Michael Brown / @breezwoods, original Anthropic Opus 4.6 hackathon winner). PermitMonkey origin is [github.com/Merrmerr21/permitmonkey](https://github.com/Merrmerr21/permitmonkey).
- The full AI ADU permit-assistant codebase mirrors the upstream's architecture: Next.js 16 + React 19 + shadcn/ui + Tailwind 4 frontend on Vercel; Express 5 orchestrator on Cloud Run; Vercel Sandbox for ephemeral agent runs; Supabase Postgres + Realtime + Storage; Claude Agent SDK + Skills.
